[tool.poetry]
name = "asteroids"
version = "0.1.0"
description = ""
authors = ["micha-aucoin <maucoin94@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pygame = "^2.6.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
